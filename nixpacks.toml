# Nixpacks configuration for XDC Lending Protocol

providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "yarn", "nginx"]

[phases.install]
cmds = ["corepack enable", "yarn install --immutable"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "nginx -c /app/nginx.conf"
